[package]
name = "cartos"
version = "0.1.0"
edition = "2024"

[dependencies]

[lib]
name = "kernel"
path = "kernel/lib.rs"  
crate-type = ["staticlib"]
test = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"